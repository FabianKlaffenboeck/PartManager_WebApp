stages:
  - build
  - package
  - deploy


build:
  stage: build
  image: node:latest
  script:
    - npm install -g @angular/cli@15.1.6
    - npm install
    - ng build
  artifacts:
    paths:
      - dist/
    expire_in: 2 hours
  cache:
    paths:
      - node_modules/

docker-build:
  image: docker:stable
  stage: package
  services:
    - docker:dind
  before_script:
    - echo $CI_BUILD_TOKEN | docker login -u "$CI_REGISTRY_USER"
      --password-stdin $CI_REGISTRY
  script:
    - docker build -t creg01.klaffenboeck.eu/partmanager/webapp .
    - docker push creg01.klaffenboeck.eu/partmanager/webapp
